These functions are now written in x86_64 Assembly (for NASM) for better performances, or written in C for a cross-system compatibility.
You can find them in the sources directory under the same name, with the ".s" or ".c" extensions
